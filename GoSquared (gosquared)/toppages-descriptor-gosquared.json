{
	"name" : "GoSquared Top Content",
	"description": "Understand which website pages are generating the most traffic in the last 30 days.",

	"datasources" : {
		"goSquared" : {
			"type": "simple_rest",
			"format": "csv",
			"name": "GoSquared Top Pages",
			"libraryAlias": "",
			"refreshInterval": 14400,
			"description": "Understand which website pages are generating the most traffic in the last 30 days.",
			"props": {
				"endpoint_url": "https://api.gosquared.com/trends/v2/page?api_key={{auth.gosquared.apiKey}}&site_token={{auth.gosquared.token}}&format=csv&from={date.add(-30).format()}&to={date.today}",
				"method": "GET"
			}
		}
	},

	"dsMappings": {
		"goSquared": "e8fe261e6c98e0d97bd3niktawuzhere"
	},
	"services" : [
		{
			"service" : "gosquared"
		}
	]

}